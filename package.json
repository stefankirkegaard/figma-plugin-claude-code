{
  "name": "figma-to-claude-plugin",
  "version": "0.1.0",
  "private": true,
  "description": "Figma plugin for design + asset operations and motion export to GIF / MP4 / PNG sequence",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "test": "node test/run.mjs",
    "test:video": "node test/video.mjs",
    "test:bridge": "node test/bridge.mjs",
    "bridge": "node bridge/server.mjs --standalone"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.101.0",
    "esbuild": "^0.25.0",
    "omggif": "^1.0.10",
    "playwright-core": "^1.62.1",
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "mp4-muxer": "^5.2.2",
    "ws": "^8.21.3"
  }
}
